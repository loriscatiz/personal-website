function Footer() {
    return (
        <footer className="bg-sky-200 text-sky-950 dark:bg-sky-900 dark:text-sky-100">
            <div className="container py-4 text-center">
                The form is currently not working, no user data is being
                collected{' '}
            </div>
        </footer>
    );
}

export default Footer;
